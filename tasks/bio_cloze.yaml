group: robustcbrn
task: bio_cloze
output_type: multiple_choice
dataset_path: ./data/toy_bio_eval.jsonl

# Verified cloze: model sees stem only; each option is scored independently
doc_to_text: "{{ doc.question }}\nAnswer:"
doc_to_choice: "{{ doc.choices }}"
doc_to_target: "{{ doc.choices[doc.answer] }}"
fewshot_num: 0
